import * as React from 'react'

// Placeholder: no user menu in the current shell specification.
// This component exists to keep the shell API extensible.

export function UserMenu() {
  return null
}

